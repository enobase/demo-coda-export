{
  "name": "@enobase/coda-export",
  "version": "0.1.0",
  "description": "Convert neobank CSV exports (Revolut, Qonto) to Belgian CODA bank statement format",
  "main": "src/index.ts",
  "module": "src/index.ts",
  "types": "src/index.ts",
  "type": "module",
  "license": "MIT",
  "bin": {
    "coda-export": "./src/cli.ts"
  },
  "scripts": {
    "test": "bun test",
    "lint": "bunx biome check src/",
    "lint:fix": "bunx biome check --write src/",
    "format": "bunx biome format --write src/"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.6",
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
